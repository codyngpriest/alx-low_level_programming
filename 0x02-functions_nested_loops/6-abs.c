#include "main.h"


/**
 * _abs - computes the absolute value of an integer
 *
 * @n: the type of the variable to compute
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
return ((n < 0) ? -n : n);
}
