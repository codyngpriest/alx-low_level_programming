#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char myString[100] = "with proper grammar, but the outcome is a piece of art,";

printf("%s\n", myString);
return (0);
}
