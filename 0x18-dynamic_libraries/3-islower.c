#include "main.h"

/**
 * _islower - checks if a character is lowercase
 *
 * @c: the character to check
 *
 * Return: Always 0 (Success)
 */

int _islower(int c)
{

return ((c >= 'a' && c <= 'z') ? 1 : 0);

return (0);
}
